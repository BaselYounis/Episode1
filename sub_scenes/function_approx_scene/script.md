There are two ways to find the function

1-A sequence of assumptions & logical steps to reach a closed form solution

let's take for example the simple problem of determining the position of a ball thrown in the air

we begin with the equation F = mass_of_ball * a

hence a = F/mass_of_ball

F = mass_of_ball * mass_of_earth * universal_gravitional_constant / r^2 

hence 

a = mass_of_ball * mass_of_earth * universal_gravitional_constant / r^2 * 1/mass_of_ball

hence 

a = mass_of_earth * universal_gravitional_constant / r^2

since the maximum height of the ball is siginificantly small relative to the radius of the earth

the change in r is small enough to be discarded and r can be assumed to be constant

hence 

a = mass_of_earth * universal_gravitional_constant /r_constant^2

constant times constant divided by a constant is also a constant

and we shall call this constant: g

hence 

a = g

by integrating the v = gt + v_zero

by integrating again p = 1/2 * g * t^2 + v_zero * t + p_zero